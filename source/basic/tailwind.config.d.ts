// Declarations for the untyped ./tailwind.config.js (imported by
// src/tailwind.config.test.ts). Keep in sync with the .js file.
import type { Config } from 'tailwindcss';

declare const tailwindConfig: Config;
export default tailwindConfig;
