import type { SmartEditState } from '@wellsfargo-starui/core';
import { INITIAL_SMART_EDIT } from '@wellsfargo-starui/core';

/** Default smart-edit settings for the lab Smart Edit tab. */
export const SMART_EDIT_TAB_STATE: SmartEditState = structuredClone(INITIAL_SMART_EDIT);
