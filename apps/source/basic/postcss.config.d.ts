// Declarations for the untyped ./postcss.config.js (imported by
// src/postcss.config.test.ts). Keep in sync with the .js file.
declare const postcssConfig: {
  plugins: Record<string, Record<string, unknown>>;
};
export default postcssConfig;
