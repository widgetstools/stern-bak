{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["host-data/dist/**"]
    },
    "test": {
      "dependsOn": ["build", "^build"],
      "outputs": ["junit.xml", "coverage/**"]
    },
    "typecheck": {
      "dependsOn": ["build", "^build"]
    }
  }
}
