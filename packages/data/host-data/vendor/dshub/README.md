Vendored rangrez `hub-rust/pkg` (`RustHub` WASM). Do not take `dshub-hub` tgz or Perspective leftovers.

Instantiated from the SharedWorker via `new URL('./dshub_bg.wasm', import.meta.url)` after `buildWorker.mjs` copies the binary next to `data-services-worker.mjs`.
