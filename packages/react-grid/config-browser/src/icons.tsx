/**
 * Icons for config-browser — the design-system `DynamicIcon` (single
 * curated Lucide map + `mkt:` market SVGs + Iconify CDN fallback). The
 * private Lucide fork that lived here was folded into the design-system
 * map (its nine unique keys added there).
 */
export { DynamicIcon, type DynamicIconProps } from '@wellsfargo-starui/design-system/icons/react';
