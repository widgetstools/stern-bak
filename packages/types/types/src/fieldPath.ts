/**
 * Field-path grammar — single source of truth lives in
 * `@wellsfargo-starui/types/shared/fieldPath`. Re-exported here so
 * `@wellsfargo-starui/types` consumers share the one parser.
 */
export * from '@wellsfargo-starui/types/shared/fieldPath';
