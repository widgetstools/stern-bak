/**
 * Data-provider types — single source of truth lives in
 * `@wellsfargo-starui/types/shared`. Re-exported here so existing `@wellsfargo-starui/types`
 * consumers keep their import paths while the definitions stay unified
 * (see docs/ARCHITECTURE.md "Foundation types").
 */
export * from '@wellsfargo-starui/types/shared/dataProvider';
