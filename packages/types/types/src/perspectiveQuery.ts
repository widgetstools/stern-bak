/**
 * Perspective worker-query wire shapes — single source of truth lives in
 * `@wellsfargo-starui/types/shared`. Re-exported here so `@wellsfargo-starui/types`
 * consumers keep one import path while the definitions stay unified
 * (see docs/ARCHITECTURE.md "Foundation types").
 */
export * from '@wellsfargo-starui/types/shared/perspectiveQuery';
