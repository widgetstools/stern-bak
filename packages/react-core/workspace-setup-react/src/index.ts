/**
 * @wellsfargo-starui/react/workspace-setup — public surface.
 *
 * The 3-pane WorkspaceSetup editor,
 * hosted in OpenFin child windows. These two components are the whole
 * public API; the dock/registry hooks and icon helpers they compose are
 * internal.
 */

export { WorkspaceSetup } from "./WorkspaceSetup";
