{
  "//": "Build shim, NOT a workspace member — @wellsfargo-starui/core is the only package in this bucket. vite-plugin-dts walks up from the entry to the nearest package.json for the types-entry path; without this file it finds the bucket manifest and resolves its './engine/dist/index.d.ts' against engine/ itself, so the declaration rollup fails on engine/engine/dist. Excluded from the packed tarball (bucket 'files' lists engine/dist only).",
  "name": "core-engine-build-shim",
  "private": true,
  "type": "module"
}
